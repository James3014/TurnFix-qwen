# 使用 nginx 作為基礎鏡像
FROM nginx:alpine

# 複製 nginx 配置
COPY nginx.conf /etc/nginx/nginx.conf

# 複製 index.html 和其他靜態文件
COPY index.html /usr/share/nginx/html/index.html

# 暴露端口
EXPOSE 80

# 啟動 nginx
CMD ["nginx", "-g", "daemon off;"]